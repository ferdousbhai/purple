{
  "name": "purple",
  "version": "0.4.0",
  "description": "AI-powered music production \u2014 Cursor for music",
  "license": "MIT",
  "packageManager": "pnpm@10.27.0",
  "type": "module",
  "scripts": {
    "start": "tauri dev",
    "dev": "tauri dev",
    "dev:webview": "vite",
    "build": "tauri build",
    "build:webview": "vite build",
    "tauri": "tauri",
    "test": "vitest run",
    "test:rust": "cargo test --manifest-path src-tauri/Cargo.toml",
    "format:rust": "cargo fmt --all --manifest-path src-tauri/Cargo.toml -- --check",
    "lint:rust": "cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings",
    "typecheck": "tsc --noEmit",
    "web:dev": "pnpm --filter @purple/web run dev",
    "web:deploy": "pnpm --filter @purple/web run deploy",
    "web:check": "pnpm --filter @purple/web run check",
    "check": "pnpm run lint && pnpm run format:rust && pnpm run lint:rust && pnpm run test && pnpm run test:rust && pnpm run typecheck && pnpm run build:webview && pnpm run web:check",
    "lint": "oxlint"
  },
  "dependencies": {
    "@purple/core": "workspace:*",
    "@purple/ui": "workspace:*",
    "@tauri-apps/api": "^2.11.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@oxlint/plugins": "1.78.0",
    "@tailwindcss/vite": "^4.3.3",
    "@tauri-apps/cli": "^2.11.4",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.0.1",
    "oxlint": "1.78.0",
    "react-resizable-panels": "^4.11.0",
    "tailwindcss": "^4.3.3",
    "typescript": "^7.0.2",
    "vite": "^8.0.10",
    "vitest": "^4.1.5"
  }
}
