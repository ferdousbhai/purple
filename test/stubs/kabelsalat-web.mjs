// Test stub: @strudel/core/repl.mjs imports SalatRepl from @kabelsalat/web,
// whose real module only resolves inside a browser bundle. Tests never touch
// the kabelsalat integration, so an inert class is enough.
export class SalatRepl {}
