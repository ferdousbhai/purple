{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Purple main window: Tauri core APIs only. Every backend call goes through an explicit command.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
